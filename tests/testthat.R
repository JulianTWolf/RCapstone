library(testthat)

expect_that(geom_timeline_label(), throws_error())
expect_that(eq_clean_data(), throws_error())
expect_that(eq_create_label(), throws_error())
expect_that(eq_location_clean(), throws_error())
expect_that(eq_map(), throws_error())
expect_that(geom_timeline(), throws_error())
